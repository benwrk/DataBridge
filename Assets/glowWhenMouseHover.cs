using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class glowWhenMouseHover : MonoBehaviour {

    private Color startcolor;
    public GameObject mainPlayer;

    //////////////void OnMouseEnter()
    //////////////{

    //////////////    if ((Vector3.Distance(gameObject.transform.position, mainPlayer.transform.position) < 3) && gameManager.ifPicked)
    //////////////    {
    //////////////        startcolor = GetComponent<Renderer>().material.color;

    //////////////        GetComponent<Renderer>().material.color = Color.yellow;
    //////////////    }

    //////////////}


    //////////////void OnMouseExit()
    //////////////{
    //////////////    if ((Vector3.Distance(gameObject.transform.position, mainPlayer.transform.position) > 3))
    //////////////        GetComponent<Renderer>().material.color = startcolor;

    //////////////}




















}
